services:
  empresa-segura:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: empresa-segura
    environment:
      PORT: 8080
    ports:
      - "${APP_PORT:-8080}:8080"
    restart: unless-stopped
